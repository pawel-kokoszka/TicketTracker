@using TicketTracker.Application.Comments;
@model IEnumerable<CommentDetailsDto>

<div>
    @{
        int commentNumber = 1;
    }

    <ul class="nav nav-pills mb-4 nav-justified shadow" id="pills-tab" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link custom-pill active" id="pills-home-tab" data-bs-toggle="pill" data-bs-target="#pills-home" type="button" role="tab">User Comments</button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link custom-pill" id="pills-profile-tab" data-bs-toggle="pill" data-bs-target="#pills-profile" type="button" role="tab">Ticket History</button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link custom-pill" id="pills-contact-tab" data-bs-toggle="pill" data-bs-target="#pills-contact" type="button" role="tab">Combined View</button>
        </li>
    </ul>
    <div class="tab-content" id="pills-tabContent">
        <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab" tabindex="0">
            @foreach (var comment in Model)
            {
                <div class="card text-bg-primary mb-2" style="">
                    <div class="card-header pt-2 pb-1 ">

                        <div class="row">
                            <label class="col text-sm">#@commentNumber    @comment.CreatedDate </label>

                            <label class="col text-sm-end">Created by: @comment.UserName</label>
                        </div>

                    </div>

                    <div class="card-body  text-wrap text-break">

                        @Html.Raw(@comment.Message!.Replace("\n", "<br />"))

                    </div>
                </div>

                commentNumber++;
            }
        </div>
        <div class="tab-pane fade" id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab" tabindex="0">...</div>
        <div class="tab-pane fade" id="pills-contact" role="tabpanel" aria-labelledby="pills-contact-tab" tabindex="0">...</div>

                

    </div>

</div>
